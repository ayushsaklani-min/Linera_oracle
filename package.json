{
  "name": "synapsenet-linera",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:wasm": "linera project build && node scripts/copy-wasm.mjs",
    "start:local": "linera local start --with-mock-validators",
    "publish:module": "linera project publish",
    "create:application": "linera project create",
    "run:application": "linera project run"
  }
}



